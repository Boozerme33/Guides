### October 03, 2024
##transform

@flatfile/plugin-graphql-schema@1.3.1
This release updates the `@flatfile/api` dependency to improve regional support. The `@flatfile/plugin-space-configure` dependency has been updated from version 0.6.0 to 0.6.1. The peer dependency `@flatfile/listener` has been updated to version 1.1.0, which may require updates to the consuming application.