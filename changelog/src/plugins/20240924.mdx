### September 24, 2024
##core_plugin

@flatfile/plugin-space-configure@0.6.0
This release updates the @flatfile/plugin-space-configure package to version 0.6.0. It includes a minor change where several Flatfile dependencies are updated to their latest versions. Additionally, there is a patch change where the @flatfile/plugin-job-handler dependency is updated to version 0.6.0. The peer dependencies @flatfile/api and @flatfile/listener are also updated to versions 1.9.13 and 1.0.5 respectively. The devDependencies @flatfile/rollup-config and @flatfile/utils-testing are updated to versions 0.1.1 and 0.3.0 respectively. Developers using this package should ensure they update their dependencies accordingly to maintain compatibility.