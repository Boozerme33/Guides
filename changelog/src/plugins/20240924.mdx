### September 24, 2024
##extractors

@flatfile/plugin-pdf-extractor@0.3.0
This release updates the `@flatfile/util-file-buffer` dependency to version 0.4.0 and the `@flatfile/util-common` dependency to version 1.4.0. It also updates the peer dependency `@flatfile/listener` to version 1.0.5, though this change may not directly impact consumers of this package. Developers using this package should ensure they have compatible versions of these dependencies installed.